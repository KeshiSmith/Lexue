<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zw.lexue">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activitys.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activitys.MainActivity" />
        <activity android:name=".activitys.UserActivity" />
        <activity android:name=".activitys.TagsActivity" />
        <activity android:name=".activitys.AboutActivity" />
        <activity android:name=".activitys.LoginActivity" />
        <activity android:name=".activitys.ReviewActivity" />
        <activity android:name=".activitys.CommonActivity" />
        <activity android:name=".activitys.ArticleActivity" />
        <activity android:name=".activitys.PasswordActivity" />
        <activity android:name=".activitys.RegisterActivity" />
        <activity android:name=".activitys.SettingsActivity" />
        <activity android:name=".activitys.ArticleListActivity" />
        <activity android:name=".activitys.UserDetailsActivity" />
    </application>

</manifest>